# Where `cargo test -p nebula-core --features ts` writes the TypeScript
# bindings. One place, so no type carries its own path; `relative = true`
# resolves it against this file's parent (the repo root).
[env]
TS_RS_EXPORT_DIR = { value = "apps/desktop/src/types", relative = true }
