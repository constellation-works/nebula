/** Where the whole-corpus DAG goes. Built in the next task, on `api.graph()`. */
export function GraphView() {
  return (
    <section className="placeholder" aria-label="Graph">
      <p>The graph view is not built yet.</p>
    </section>
  );
}
